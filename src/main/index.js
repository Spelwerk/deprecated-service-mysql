const Association = require('../classes/Association');
const Connection = require('../classes/Connection');
const Query = require('../classes/Query');
const Relation = require('../classes/Relation');

module.exports = {
    Association,
    Connection,
    Query,
    Relation,
};
